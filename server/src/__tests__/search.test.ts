import supertest from "supertest";

describe("search route", () => {
  describe("not given the search query", () => {
    it("should return a error message", () => {
      expect(true).toBe(true);
    });
  });
});
